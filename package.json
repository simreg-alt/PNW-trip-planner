{
  "name": "regal-trip-command-center",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 migrations apply regal-trip --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
